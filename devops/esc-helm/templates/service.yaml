apiVersion: v1
kind: Service
metadata:
  name: {{ include "esc-helm.fullname" . }}
  labels:
    {{- include "esc-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "esc-helm.selectorLabels" . | nindent 4 }}
