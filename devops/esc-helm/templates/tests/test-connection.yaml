apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "esc-helm.fullname" . }}-test-connection"
  labels:
    {{- include "esc-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "esc-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
