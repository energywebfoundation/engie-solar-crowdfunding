export const useLendingTermsEffects = () => {
  const listItems: string[] = [
    'EWT contributions may not be withdrawn until the underlying loans mature, after 1 year',
    'EWT contributions are exchanged for Solar Loan Tokens (SLT). You will use SLT to redeem your loan + reward at the end of the program',
    'A small gas fee will be charged for LEND and REDEEM operation',
  ];
  return listItems;
};
